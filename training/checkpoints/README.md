Place the released base `boltz1.ckpt` here. After monomer training, copy the
selected `mon-FT.ckpt` here before starting complex fine-tuning. Large model
files are distributed separately and are intentionally not included in this
code archive.
