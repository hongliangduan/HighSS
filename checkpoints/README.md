# HighSS checkpoint files

The source archive intentionally excludes model binaries.
Place these two files here before running HighSS:

- `best-model-epoch68-rmsd5.0984.ckpt`
- `ccd.pkl`

`boltz1_conf.ckpt` is not required.
